version: '3'
services:
  spark:
    image: bitnami/spark:3.2
    container_name: spark
    environment:
      - SPARK_MODE=master
    ports:
      - "7077:7077"
      - "8081:8080"
    networks:
      - airflow_default

networks:
  airflow_default:
    external: true
